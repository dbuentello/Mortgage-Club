dependencies:
  cache_directories:
    - pdftk
  pre:
    - if [[ ! -e pdftk ]]; then sudo apt-get install pdftk; fi
test:
  pre:
    - bundle exec rake assets:webpack
  post:
    - bundle exec rubocop
general:
  artifacts:
    - "~/simplecov" # relative to the user's home directory
